# hadolint configuration
# https://github.com/hadolint/hadolint
#
# No global ignores — all suppressions are inline in the Dockerfile
# so the reason is visible at the suppression site.

trustedRegistries:
  - docker.io
  - gcr.io
  - ghcr.io
